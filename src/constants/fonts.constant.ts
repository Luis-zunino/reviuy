import { Inter } from 'next/font/google';

/**
 * AUDYR TYPOGRAPHY: Inter
 * Primary typeface for all text elements. Clean, functional,
 * with slight negative letter-spacing for a refined, professional look.
 */
export const manrope = Inter({
  subsets: ['latin'],
  weight: ['400', '500', '600', '700', '800'],
  display: 'swap',
});

export const playfair = Inter({
  subsets: ['latin'],
  weight: ['500', '600', '700'],
  display: 'swap',
});
